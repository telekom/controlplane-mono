# permissions for end users to view consumeroutes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/managed-by: kustomize
  name: consumeroute-viewer-role
rules:
- apiGroups:
  - gateway.cp.ei.telekom.de
  resources:
  - consumeroutes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - gateway.cp.ei.telekom.de
  resources:
  - consumeroutes/status
  verbs:
  - get
